
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model Leads {
  id String @id @default(uuid())
  name  String
  email String
  mobileNumber  String
  city    String
}